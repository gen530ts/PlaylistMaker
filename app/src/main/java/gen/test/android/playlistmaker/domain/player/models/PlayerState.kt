package gen.test.android.playlistmaker.domain.player.models

enum class PlayerState {
    STATE_DEFAULT,
    STATE_PREPARED,
    STATE_PLAYING,
    STATE_PAUSED,
    STATE_COMPLETED,
    STATE_STOPPED
}